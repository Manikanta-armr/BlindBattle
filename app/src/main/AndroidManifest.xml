<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlindBattle"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PlayerNamenew"/>
        <activity android:name=".PlayWithBotOptionSelection"/>
        <activity android:name=".PlayNamebot"/>
        <activity android:name=".BotIntro"/>
        <activity android:name=".NewGameActivity"/>
        <activity android:name=".NewGameIntroActivity"/>
        <activity android:name=".Easy_Blind_Mode"/>
        <activity android:name=".Medium_Blind_Mode"/>
        <activity android:name=".Hard_Blind_Mode"/>
    </application>

</manifest>